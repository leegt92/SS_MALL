<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.bit.ssmall.mapper.RegisterMapper">
	
	
	
	<insert id="register" parameterType="edu.bit.ssmall.vo.RegisterRequest">
		<![CDATA[ 
        Insert into member(m_number, m_name, m_age, m_email, m_adress, m_phonenum, m_point, m_receive_email, m_authority, m_id, m_password, m_price, m_enabled)
        values(member_seq.nextval,#{regReq.m_name},#{regReq.m_age},#{regReq.m_email} , #{regReq.m_adress}, #{regReq.m_phonenum}, 0, '수신', '일반회원', #{regReq.m_id} ,#{regReq.m_password}, 0, 1)
		]]>
	</insert>
	
	<select id="idChk" resultType="edu.bit.ssmall.vo.MemberVO" parameterType="edu.bit.ssmall.vo.RegisterRequest">
		<![CDATA[
		Select m_id FROM MEMBER WHERE m_id = #{regReq.m_id}
		]]>
	</select>
	
</mapper>
